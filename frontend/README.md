# Frontend (Next.js)

Apple-2026-inspired Liquid Glass frontend for EcoFlow sizing workflows.

## Routes
- `/` dashboard + input + analysis + export
- `/showcase` component showcase (acts as lightweight Storybook substitute)

## Run
```bash
npm install
npm run dev
```

Set API URL:
```bash
NEXT_PUBLIC_API_URL=http://localhost:8000
```
